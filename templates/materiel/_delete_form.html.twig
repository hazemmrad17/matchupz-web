<form method="post" action="{{ path('app_materiel_delete', {'id_materiel': materiel.id_materiel}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ materiel.id_materiel) }}">
    <button class="btn">Delete</button>
</form>
